# Chatbot Frontend

This frontend uses the Next.js App Router (`app/`).

Note: an empty `pages/` directory is intentionally retained to avoid dev-container `ENOENT` scans in Next's watcher. It is not used for routing.
